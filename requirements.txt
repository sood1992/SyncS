# SyncFox - Audio-Video Sync Tool Dependencies

# Core DSP
numpy>=1.24
scipy>=1.10
librosa>=0.10

# GUI
PySide6>=6.5
# Note: PyQt-Fluent-Widgets removed - requires PyQt5, incompatible with PySide6

# Timeline
opentimelineio>=0.15

# Audio fingerprinting
pyacoustid>=1.2

# Utilities
tqdm>=4.65
appdirs>=1.4

# Packaging
pyinstaller>=5.0

# Note: Also requires system installs:
# - FFmpeg (for audio extraction)
# - Chromaprint/fpcalc (for audio fingerprinting)
